#!/usr/bin/env node

import { promises } from 'fs'

import // join,
// basename,
// dirname,
// extname,
// relative,
// isAbsolute,
// resolve,
// sep,
'path'

const pathCopy = process.env.PWD
console.log("pathCopy", pathCopy);

// promises.copyFile('./', pathCopy)
